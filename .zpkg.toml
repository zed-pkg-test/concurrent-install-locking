[package]
org = "zed-pkg-test"
name = "concurrent-install-locking"
version = "0.1.0"
description = "Independent chaos acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/zed-pkg-test/concurrent-install-locking"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]

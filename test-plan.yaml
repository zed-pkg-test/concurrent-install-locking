schema_version: 1
managed_by: github-test-org-factory/1.0.0
production_org: zed-pkg
test_org: zed-pkg-test
repository: concurrent-install-locking
kind: chaos
readiness: ready
objectives:
- Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance across the supported
  happy-path states and canonical fixtures.
- Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance under retries,
  interruption, concurrency, offline operation, or partial failure.
- Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance preserves authorization,
  idempotency, integrity, observability, and actionable failure classification.
always_on_checks:
- Validate test-plan and dependency metadata.
- Validate generated fixture and workflow syntax.
- Classify missing upstreams or credentials separately from product regressions.
schedule_utc: 17 5 * * *
expensive: true
required_infrastructure:
- multi-process runner
cases:
- id: TC-001
  title: Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance across
    the supported happy-path states and canonical fixtures.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-002
  title: Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance under
    retries, interruption, concurrency, offline operation, or partial failure.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-003
  title: Verify five concurrent installs, per-artifact locks, killed-owner recovery, and corruption resistance preserves
    authorization, idempotency, integrity, observability, and actionable failure classification.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
